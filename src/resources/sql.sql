drop table config

create table config( proteId varchar(200) NOT NULL , descr VARCHAR(500), ip varchar(200),port varchar(200), cmsip VARCHAR(500), cmsport varchar(200),cmssysport varchar(200),sysport varchar(200), PRIMARY KEY (proteId));